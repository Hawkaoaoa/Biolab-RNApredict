# include <stdio.h>
# include <stdlib.h>
# include <time.h>
# include <string.h>

#define CROSS 0.35
#define MUTATE 0.05     
#define MAXITER 25  

typedef enum {false,true} bool;

struct mysample{
    int start;
    int end;
    int length;
}sample[1000];
 
